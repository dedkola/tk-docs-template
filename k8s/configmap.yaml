apiVersion: v1
kind: ConfigMap
metadata:
  name: tk-docs-config
  labels:
    app: tk-docs
data:
  # Add any configuration values here
  # These can be mounted as files or used as environment variables
  NODE_ENV: "production"
  # Example: Add your site URL, analytics ID, etc.
  # NEXT_PUBLIC_SITE_URL: "https://docs.example.com"
  # NEXT_PUBLIC_GA_ID: "G-XXXXXXXXXX"
